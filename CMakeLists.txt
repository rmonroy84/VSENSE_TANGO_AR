# Main CMake script to be used on Windows only
CMAKE_MINIMUM_REQUIRED(VERSION 3.9)

ADD_DEFINITIONS(-DNOMINMAX)
SET(CMAKE_CXX_FLAGS_DEBUG "-DVSENSE_DEBUG ${CMAKE_CXX_FLAGS_DEBUG}")

IF(MSVC)
	ADD_SUBDIRECTORY(vsense-libs)
ENDIF()